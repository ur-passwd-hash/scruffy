interface:
  display_name: "Scruffy"
  short_description: "Repair approved Scruffy findings and verify them"
  default_prompt: "Use Scruffy's compatibility repair entrypoint $scruffys-mop to implement approved findings: read findings.json, context.json, and decisions.json, apply work orders in dependency order under explicit source-write authority, and hand the changed surfaces back for Scruffy re-audit."
policy:
  allow_implicit_invocation: true
