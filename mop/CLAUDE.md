@AGENTS.md

# Claude Code

For local runtime tests, launch this checkout with `claude --plugin-dir .` and
invoke `/scruffys-mop:scruffys-mop`. Scruffy is the parent repository; its
`references/output-schema.md` and `schema/audit-contract.json` are the contract
Scruffy's Mop consumes read-only. Keep machine-specific notes in `CLAUDE.local.md`.
